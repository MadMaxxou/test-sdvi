{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block body %}
    <h1 style="text-align:center">Traçabilité RFID - Site d'administration</h1>

    <div class="login-form">
    <form action="{{ path('login') }}" method="post">
        <h2 class="text-center">Connexion</h2>
        <div class="form-group">
            <input placeholder="identifiant" class="form-control" type="text" id="username" name="_username" value="{{ last_username }}" />
        </div>
        <div class="form-group">
            <input placeholder="Mot de passe" class="form-control" type="password" id="password" name="_password" />
        </div>
        {% if error %}

            <p style="color:red">Identifiant ou mot de passe incorrect.</p>
        {% endif %}
        <div class="form-group">
            <button type="submit" class="btn btn-success btn-block">Se connecter</button>
        </div>

    </form>
{% endblock %}